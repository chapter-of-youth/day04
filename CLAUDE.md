# JY 포트폴리오

## 프로젝트 소개
- **프로젝트명**: JY 포트폴리오
- **목적**: 디자인 작업물을 소개하는 개인 포트폴리오 사이트

## 핵심 기능
- 자기소개
- 작업물 목록
- 작업물 상세
- 로그인
- 작업물 등록 / 수정 / 삭제

## 현재 상태
현재는 위 기능을 향한 연습 단계로, 정적 HTML 페이지와 Supabase 연동을 익히는 중이다.

- `index.html` — 시작 페이지, 다른 페이지로의 링크 포함
- `about.html` — 소개 페이지
- `preview.html` — Supabase `test_todos` 테이블 데이터를 목록으로 렌더링하는 데모 페이지
- `style.css` — 전 페이지 공통 스타일(파란 계열 그라데이션 배경, 카드형 레이아웃)

## 데이터베이스
- Supabase REST API를 통해 데이터를 불러온다 (`preview.html` 참고).
- 등록/수정/삭제 기능을 구현할 때도 동일한 Supabase 프로젝트를 사용할 예정이다.

## 작업 시 참고사항
- 새 페이지를 추가할 때는 `style.css`를 링크하여 기존 디자인(그라데이션 배경 + `.card` 레이아웃)과 톤을 맞춘다.
- 페이지 간 이동은 `.nav-links` 영역에 링크를 넣는 기존 패턴을 따른다.
- 핵심 기능(자기소개, 작업물 목록/상세, 로그인, 작업물 CRUD)을 구현해 나가며 이 문서를 갱신한다.

## 폰트 규칙
- 폰트 컬러는 #111로 설정한다.
